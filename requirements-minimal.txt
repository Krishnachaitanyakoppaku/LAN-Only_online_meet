# Minimal requirements for basic functionality
# These packages should work on most systems

# Core dependencies
opencv-python>=4.8.0
numpy>=1.26.0
Pillow>=10.0.0

# Audio (optional - may require system dependencies)
# pyaudio>=0.2.11

# GUI enhancements (optional)
# customtkinter>=5.2.0

# Note: PyAudio may require additional system packages:
# Ubuntu/Debian: sudo apt-get install python3-pyaudio portaudio19-dev
# CentOS/RHEL: sudo yum install python3-pyaudio portaudio-devel
# macOS: brew install portaudio
# Windows: Usually works with pip install
